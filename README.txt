2018/10/14

draw_line.py:			is a program allow user to define a line, and the program can know the (x1,y1) and (x2,y2) location

yolo(VM_copy).py:		file that I used to edit and test the code.

yolo(v1.0_success).py		Succesfully only recognize 'car' 'trunk' and 'motorbike'.

yolo(v1.1_success_counting).py:	Succefully add the counting feature to the program.